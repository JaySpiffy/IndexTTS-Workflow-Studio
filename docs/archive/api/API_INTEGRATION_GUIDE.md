# Archived API Integration Guide

This older guide was moved out of the active docs set because it drifted from the current route structure, port defaults, and Docker-first runtime.

Use the current API summary instead:

- [../../api/API_README.md](../../api/API_README.md)

If you need the original historical content, recover it from version history.
