# Test Run Log

## Summary

- Date: 2026-04-10
- Tester: Codex
- Purpose: Full release-style smoke pass across the main app surfaces after the testing-docs rollout and the conversation workflow success-path fix
- App build or bundle version: `2026-04-10-release-smoke-1`
- Runtime: Docker, frontend `http://localhost:3000`, backend `http://localhost:8001`
- Result: Pass

## Environment

- Frontend URL: `http://localhost:3000`
- Backend URL: `http://localhost:8001`
- Device mode: `cuda:0`
- DeepSpeed: `true`
- Local voices present: yes, 8 active speaker files loaded at the end of the pass
- Script packs used:
  - manual two-line release smoke script entered in the workflow
  - direct standalone timeline text entered in the timeline editor

## Packs Run

- [x] `00-stack-startup-smoke`
- [x] `01-speaker-prep-smoke`
- [x] `02-conversation-workflow-smoke`
- [x] `03-conversation-results-smoke`
- [x] `04-timeline-editor-smoke`
- [x] `full-release-checklist`

## Results By Area

| Area | Status | Notes |
| --- | --- | --- |
| Stack startup | Pass | Frontend and backend both came up healthy. Verified via `http://localhost:3000/api/health` and `http://localhost:8001/health`. |
| Speaker Prep | Pass | `Pr.D.Trump.wav` auto-loaded in Speaker Prep, diagnostics populated, and prep completed successfully. Output helper file `Pr.D.Trump_ready.wav` was created during the smoke and then archived back out of the active speaker folder after verification. |
| Conversation Workflow | Pass | Parsed a real two-line script, generated audio successfully, progress reached `Generation completed`, and the workflow result panel appeared. A real success-path bug was found and fixed during this pass: `Generate Conversation` now re-enables after completion. |
| Conversation Results | Pass | Selected the completed conversation, verified review and pacing labels were present, confirmed export gating blocked concatenate until each line had one selection, used `Auto-Select Best`, and successfully concatenated the full mix. |
| Timeline Editor | Pass | Built a standalone blank timeline, added a speaker track, added a segment directly in the editor, generated missing segment audio, exported the timeline, and preview/download network calls returned `200`. |
| Export / playback | Pass | Conversation concatenate/export succeeded and produced `conversation_072b2025-1e90-4c3f-abc3-fdaa1ea2fc27_concatenated.wav`. Timeline export and preview/download also succeeded for project `6cb074ad-e2e3-4dc4-bf44-e2cb62560c90`. |
| Docs / manual | Pass | The testing docs, run-log template, and release smoke structure were all in place and used for this pass. |

## Browser / Runtime Evidence

### Speaker Prep

- Source clip auto-selected: `Pr.D.Trump.wav`
- Diagnostics loaded successfully
- Prep result:
  - output: `Pr.D.Trump_ready.wav`
  - before: `EXCELLENT 87`
  - after: `EXCELLENT 89`
  - trimmed duration: `49.6s`
  - notes included trimming, mono conversion, and normalization

### Conversation Workflow

- Script:
  - `Pr.D.Trump: We are doing a final release smoke today.`
  - `JoeRogan: I just want to make sure the workflow really works.`
- Conversation ID: `072b2025-1e90-4c3f-abc3-fdaa1ea2fc27`
- Generation completed in-browser with:
  - progress text: `Generation completed`
  - `Generate Conversation` re-enabled after completion
  - `View Results` available

### Conversation Results

- Loaded completed conversation from the results list
- Total lines: `2`
- Initial gating state:
  - selected lines: `0`
  - concatenate disabled: `true`
  - readiness text: `Pick exactly one version for every line before export: choose a version for lines 1, 2.`
- After `Auto-Select Best`:
  - selected lines: `2`
  - concatenate disabled: `false`
  - readiness text: `Ready to export. 2 of 2 lines have a final selection.`
- Concatenate result:
  - `Conversation concatenated successfully using volume matching.`
  - `Play Full Conversation` button present
  - `Download Full Conversation` button present

### Timeline Editor

- Timeline project ID: `6cb074ad-e2e3-4dc4-bf44-e2cb62560c90`
- Timeline project name: `release-smoke-timeline-1775817412104`
- Built directly in the editor:
  - track name: `Smoke Lead`
  - speaker: `Pr.D.Trump.wav`
  - segment text: `This timeline is being built directly inside the editor.`
- Final timeline summary:
  - `1 track | 1 segment | 2.0s`
- Verified network success for:
  - timeline create
  - track add
  - segment add
  - segment generate
  - waveform fetch
  - export
  - export download / preview

## Backend Contract Tests

| Test | Result | Notes |
| --- | --- | --- |
| `tests/backend/test_source_clip_prep_contract.py` | Pass | `4` tests passed in the live GPU-backed backend container. |
| `tests/backend/test_generation_queue_contract.py` | Pass | `2` tests passed in the live GPU-backed backend container. |
| `tests/backend/test_pacing_review_contract.py` | Pass | `2` tests passed in the live GPU-backed backend container. |
| `tests/backend/test_selection_gating_contract.py` | Pass | `4` tests passed in the live GPU-backed backend container. |
| `tests/backend/test_export_finishing_contract.py` | Pass | `3` tests passed in the live GPU-backed backend container. |
| `tests/backend/test_timeline_editor_contract.py` | Pass | `3` tests passed in the live GPU-backed backend container. |

## Bugs Or Regressions Found

1. `Generate Conversation` stayed disabled after a successful run. This was a real UI bug in `frontend/src/modules/conversationWorkflow.js`. It was fixed during this pass, the frontend bundle was rebuilt, and the success path was re-tested live.
2. One temporary runtime drift happened during rebuild: the stack initially came back on the plain compose path and reported `cpu` instead of `cuda:0`. Restarting with `docker-compose.gpu.yml` restored the intended `cuda:0 + DeepSpeed` runtime before the final verification run.

## Follow-Up Actions

1. Keep using this file as the baseline example of a full release-style manual smoke pass.
2. If future release passes touch Speaker Prep again, archive any helper `*_ready.wav` files back out of the active speaker library after smoke verification.
3. If conversation results UX is polished again later, consider auto-loading the current conversation when switching from workflow to results, since the current flow still expects selecting the conversation item in the results list.

## Sign-Off

- Release-ready: yes
- Remaining blockers: none found in this pass
- Notes for next pass: this run covered the main user-facing surfaces on `cuda:0 + DeepSpeed` with real browser interaction and container-level contract checks
